import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "PitchCraft AI - Transform Ideas into Investment-Ready Presentations",
  description:
    "AI-powered presentation generator that transforms any text document into professional, VC-ready presentations with stunning visuals and smooth transitions.",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
